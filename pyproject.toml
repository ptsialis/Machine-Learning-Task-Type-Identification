[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mltid-utils"
version = "0.1.0"
description = "Utility package for ML task identification experiments"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["utils*"]
